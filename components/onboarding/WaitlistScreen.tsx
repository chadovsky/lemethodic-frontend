'use client'

// P-222 — waitlist screen. Stub in commit 1 (renders nothing); commit 2 builds
// out copy + CTAs from useSubmitResponseStore.

export default function WaitlistScreen() {
  return null
}
