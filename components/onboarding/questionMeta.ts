// F-201 — per-question pastel cycle + per-question illustration set
// retired. P-220.z scope evaporated; EcoleReveal hero asset re-tracked
// as P-228.
//
// Question screens are now type-led on uniform `--ed-bg` (set by the
// OnboardingScreen kernel). Visual weight concentrates at EcoleReveal
// where the persona reveal earns it.

// EcoleReveal still gets a single hero illustration — placeholder until
// P-228 ships an art-directed asset. Bg remains warm off-white via
// `--ed-paper` (set inline in EcoleReveal.tsx) so the reveal moment
// reads as a "page turn" against the surrounding `--ed-bg` flow.
export const ECOLE_REVEAL_ILLUSTRATION = '/illustration-ecole.png'
export const ECOLE_REVEAL_ILLUSTRATION_ALT_EN = "L'École illustration"
export const ECOLE_REVEAL_ILLUSTRATION_ALT_FR = "Illustration L'École"
