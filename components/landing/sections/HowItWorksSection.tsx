// M-101a — how it works. Skeleton in commit 1; commit 2 builds out.

import type { Lang } from '../copy'

export default function HowItWorksSection({ lang }: { lang: Lang }) {
  void lang
  return null
}
