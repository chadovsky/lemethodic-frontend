// M-101a — FAQ. Skeleton in commit 1; commit 2 builds out with shadcn Accordion.

import type { Lang } from '../copy'

export default function FAQSection({ lang }: { lang: Lang }) {
  void lang
  return null
}
