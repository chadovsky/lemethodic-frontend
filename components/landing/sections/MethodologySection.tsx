// M-101a — methodology. Skeleton in commit 1; commit 2 builds out.

import type { Lang } from '../copy'

export default function MethodologySection({ lang }: { lang: Lang }) {
  void lang
  return null
}
