// M-101a — final CTA. Skeleton in commit 1; commit 2 builds out.

import type { Lang } from '../copy'

export default function FinalCTASection({ lang }: { lang: Lang }) {
  void lang
  return null
}
