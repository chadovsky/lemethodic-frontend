// M-101a — pricing. Skeleton in commit 1; commit 2 builds out 3 cards
// (Subscription / Sprint coming-soon / Premium coming-soon) + waitlist.

import type { Lang } from '../copy'

export default function PricingSection({ lang }: { lang: Lang }) {
  void lang
  return null
}
