// M-101a — problem. Skeleton in commit 1 (renders nothing); commit 2 builds out.

import type { Lang } from '../copy'

export default function ProblemSection({ lang }: { lang: Lang }) {
  void lang
  return null
}
