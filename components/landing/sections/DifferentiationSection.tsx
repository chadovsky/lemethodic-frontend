// M-101a — differentiation. Skeleton in commit 1; commit 2 builds out.

import type { Lang } from '../copy'

export default function DifferentiationSection({ lang }: { lang: Lang }) {
  void lang
  return null
}
