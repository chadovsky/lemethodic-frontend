// Future product page structure: cover image, title, description, price,
// format (PDF, audio, print), buy via LemonSqueezy CTA.
// Phase 2: populate when Book-Lab catalog items are ingested.

import { notFound } from 'next/navigation'

export default function LibrairieItemPage() {
  notFound()
}
